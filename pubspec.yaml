name: store_generator
description: A new Flutter project.
version: 0.0.1
homepage:
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  build:
  source_gen:
  analyzer:
  store:


dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:


